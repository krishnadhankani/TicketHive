# Optional. The backend already defaults to these values, so it runs
# without a .env file at all. Copy to .env only if you want to change something.

PORT=5050
MONGO_URI=mongodb://127.0.0.1:27017/TicketHive
JWT_SECRET=replace_with_a_long_random_string
